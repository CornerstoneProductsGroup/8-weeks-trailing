# Trailing Report (Exact Layout)

- Tabs per retailer; weeks across columns like your master file.
- Weekly uploads: one workbook with each sheet = retailer; columns: SKU, Units.
- Period selector for any start/end; totals per SKU and per retailer.

## Run
```bash
pip install -r requirements.txt
streamlit run app.py
```
